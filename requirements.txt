# PyTorch - CUDA 12.1
# pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cu121
torch==2.1.0+cu121
torchvision==0.16.0+cu121
torchaudio==2.1.0+cu121

# Core dependencies
numpy==1.24.4
opencv-python>=4.8.0
pillow>=10.0.0
pyyaml>=6.0
tqdm>=4.65.0
pandas>=2.0.0
matplotlib>=3.7.0

# Deep Learning
transformers>=4.30.0
ultralytics>=8.0.0
timm>=0.9.0
einops>=0.8.0
yacs>=0.1.8

# Evaluation
pycocotools>=2.0.6
# pycocoevalcap - install from git:
# pip install git+https://github.com/salaniz/pycocoevalcap.git

# LLM dependencies
accelerate>=0.20.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# SGG-Benchmark (install from submodule)
# cd models/sgg && pip install -e .
